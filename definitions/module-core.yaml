# Core Module Object
---
# id: Standard object id generated by platform on creation
mimeType: application/vnd.reflective-dk.object+json
registrations:
  - validity:
      - input:
          # Responsible actor - actor: { id: actor.id }
          class: class:core:module
          moduleId: module:core
          description: >-
            This module provides the core definitions to bootstrap the type and
            rule system on the platform
          dependencies: {}
...
