# Metamodel for the type and rule system on the Reflective Platform
---
# id: Standard object id generated by platform on creation
mimeType: application/vnd.reflective-dk.object+json
registrations:
  - validity:
      - input:
          # Responsible actor - actor: { id: actor.id }
          class: class:core:model
          modelId: model:metamodel
          description: >-
            This model provides the core definitions for the type and
            rule system on the Reflective Platform
          dependencies: {}
...
